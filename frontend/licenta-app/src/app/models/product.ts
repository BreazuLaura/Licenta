import {SafeUrl} from "@angular/platform-browser";

export interface Product {
  id?: number;
  name: string;
  price: number;
  description: string;
  contactInfo: string;
  category: string;
  status: string;
  saleType: string;
  dorm: string;
}

